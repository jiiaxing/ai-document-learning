# AI 课件助教开发流程（执行文档）

## 1. 目标

建立“开发 → 测试 → 通过后推进 / 不通过回滚修复”的闭环流程，减少回归。

---

## 2. 总流程

```text
需求确认
  ↓
拆分任务（一个功能一个切片）
  ↓
实现切片
  ↓
本地编译 + 静态检查
  ↓
功能测试（手工 + 自动）
  ↓
通过？
  ├─ 是：进入下一个功能切片
  └─ 否：回到实现切片修复
```

---

## 3. 功能切片顺序（建议）

## Slice 1：PDF 自建 viewer 触发制

目标：`mouseup + 选区文本` 触发成功。

测试通过条件：

- 拖选过程中不触发
- 左键松开后触发一次
- 触发文本与选区一致

失败处理：

- 先修触发逻辑，不进入 Slice 2

---

## Slice 2：统一请求编排与状态机

目标：所有请求入口统一走 orchestrator。

测试通过条件：

- 自动触发 / 手动触发均可请求
- 状态迁移无冲突

失败处理：

- 回退到上一稳定提交，修复后重测

---

## Slice 3：取消响应可靠性

目标：取消后无旧输出落地。

测试通过条件：

- 点取消后状态立即反馈
- 迟到回包不显示
- OCR 与图片请求同样可取消

失败处理：

- 仅修 `CancellationGuard` 与状态机，不改其它模块

---

## Slice 4：Provider 适配层

目标：Copilot / OpenAI 兼容 / 视觉 / OCR 回退稳定。

测试通过条件：

- 文本问答成功
- 图片讲解成功（视觉优先）
- 视觉失败可回退 OCR（若开启）

失败处理：

- 对单一 provider 修复，不影响触发层

---

## Slice 5：UI 与交互优化

目标：消息区、输入区、状态提示一致。

测试通过条件：

- 输入区固定底部
- 消息区独立滚动
- 状态提示正确

失败处理：

- 不改业务层，仅修 UI 层

---

## 4. 每个切片的标准操作

1. 新建分支（或标记工作点）
2. 只改本切片相关文件
3. 执行编译与 lint
4. 执行对应测试清单
5. 记录结果
6. 通过后再开始下一切片

---

## 5. 测试门禁

## 5.1 编译门禁

- `npm run compile` 必须通过

## 5.2 关键手工测试门禁

- 文本选区触发
- PDF 选区触发
- 取消响应
- 图片讲解
- 手动追问

## 5.3 回归门禁

每完成一个切片，至少回归以下 3 条：

- 触发是否正确
- 取消是否生效
- 旧功能是否被破坏

---

## 6. 失败回路（必须执行）

若测试不通过：

1. 停止推进下一切片
2. 回到当前切片代码修复
3. 重新执行本切片全部测试
4. 通过后才可继续

禁止“带病推进”。

---

## 7. 变更记录模板

每次切片完成后记录：

- 变更目标
- 修改文件
- 通过测试项
- 未通过项与处理方式
- 已知风险

---

## 8. 发布前检查

- 编译通过
- 关键手工测试全通过
- 无高优先级已知问题
- 文档（需求/技术/流程）与实现一致
