# PDF Reader 设计规范

本项目采用 Office / Word 风格的浅色工作台视觉。界面应当服务长时间阅读和批注：低噪声、清爽、边界明确、操作控件像工具栏一样稳定，不做营销式大卡片或强装饰背景。新增页面和新组件开发前必须先阅读本文，并优先复用这里定义的色值、字号、间距和组件规则。

## 配色

主色使用 Word 蓝，承担焦点、链接和少量当前状态提示。常规按钮和工具选中态不使用蓝色强调。

- 主色 `--accent`: `#185abd`
- 主色悬浮 `--accent-hover`: `#124a9c`
- 主色浅底 `--accent-soft`: `#eaf2ff`
- 主色边框 `--accent-line`: `#8cb6e8`

辅助色只作为语义或局部提示，不大面积铺开。

- 信息青绿 `--info`: `#2b7a78`
- 成功绿 `--success`: `#107c41`
- 警告橙 `--warning`: `#b45309`
- 危险红 `--danger`: `#b42318`

背景采用非常浅的冷暖混合渐变，模拟 Word 工作区的柔和环境光。大面积阅读区不得使用高饱和色。

- 页面背景 `--bg`: `#edf3f2`
- 背景暖色 `--bg-warm`: `#f5f0e6`
- 背景冷色 `--bg-cool`: `#eef3f7`
- 背景淡紫 `--bg-lilac`: `#f1edf7`
- 主面板 `--surface`: `#ffffff`
- 次级面板 `--surface-2`: `#f7f9fb`
- 工具栏面板 `--surface-3`: `#fbfcfd`
- 悬浮面板 `--surface-float`: `rgba(255, 255, 255, 0.96)`

文字色保持 Office 式中性灰黑，正文不使用纯黑。

- 主文字 `--ink`: `#242424`
- 正文 `--text`: `#323130`
- 次级文字 `--muted`: `#605e5c`
- 弱提示 `--subtle`: `#8a8886`
- 反白文字 `--on-accent`: `#ffffff`
- 分割线 `--line`: `#d6dce5`
- 弱分割线 `--line-soft`: `#edf0f4`

深色模式参考 VS Code 深色工作台：界面背景和面板使用近黑灰，文字、按钮和输入内容转为浅灰/白色；按钮选中态仍然不用蓝色，使用浅灰透明底和字重加粗。PDF 页面本身保持白纸效果，页面内笔记继续保持深色文字，避免深色模式破坏“写在 PDF 上”的观感。

- 深色页面背景 `--bg`: `#1e1e1e`
- 深色阅读区背景 `--bg-cool`: `#181818`
- 深色主面板 `--surface`: `#1e1e1e`
- 深色次级面板 `--surface-2`: `#252526`
- 深色工具栏面板 `--surface-3`: `#2d2d30`
- 深色悬浮面板 `--surface-float`: `rgba(37, 37, 38, 0.98)`
- 深色主文字 `--ink`: `#f3f3f3`
- 深色正文 `--text`: `#d4d4d4`
- 深色次级文字 `--muted`: `#a6a6a6`
- 深色分割线 `--line`: `#3c3c3c`
- 深色选中工具底色 `--tool-selected`: `rgba(255, 255, 255, 0.16)`
- 深色悬浮工具底色 `--tool-hover`: `rgba(255, 255, 255, 0.08)`

## 字体

字体族统一为 `"Segoe UI", "Microsoft YaHei", Arial, sans-serif`。标题和按钮使用系统 UI 字体的清晰感，PDF 内容和笔记内容不强行替换原文档字体。

- 页面基础字号：`14px`
- 工具栏按钮：`13px`，字重 `400`，行高 `1`
- 面板标题：`14px`，字重 `600`，行高 `1.35`
- 抽屉标题：`20px`，字重 `700`，行高 `1.3`
- 正文/消息：`14px`，字重 `400`，行高 `1.58`
- 次级说明：`12px`，字重 `400`，行高 `1.35`
- 列表主标题：`14px`，字重 `600`，行高 `1.35`
- 代码：`Consolas, "Cascadia Code", monospace`，字号 `0.94em`

## 间距

采用 4px 基础网格。界面整体保持紧凑，但组件之间留出清楚的呼吸感。

- 最小间距：`4px`
- 图标/文字间距：`6px`
- 工具按钮组内间距：`6px`
- 工具栏内边距：`8px 12px`
- 面板头部内边距：`0 14px`
- 表单字段间距：`10px`
- 面板正文内边距：`14px`
- 抽屉正文内边距：`20px 24px`
- 文件列表行高：`58px`
- PDF 阅读区内边距：`24px`
- 连续页面间距：`22px`

## 布局

整体布局参考 Office 文档工作台：顶部是稳定的 ribbon 工具区，中间是以 PDF 页面为视觉中心的阅读区，左右为可折叠工作面板。新增页面不得改变这一层级关系；需要新增功能时，优先放入既有工具组、侧栏表单、浮动操作条或抽屉。

- 顶栏分为文件工具栏和批注工具栏，两层都保持横向紧凑排列，允许横向滚动，不做大按钮重排。
- 阅读区居中展示 PDF 页面，连续模式用垂直页面栈，页面之间保持 `22px` 间距。
- 左侧翻译栏和右侧 AI 栏使用同一面板语言：50px 头部、正文 14px 内边距、细分割线。
- 设置与文件列表使用抽屉/侧滑面板，宽度随视口收缩，但不覆盖核心控件的视觉规则。
- 文本选择、视觉选择和笔记编辑工具条属于临时浮层，应贴近操作目标，小尺寸、轻阴影、白底细边框。

## 组件样式

按钮使用 Word / Edge 式工具按钮语言。顶部工具栏、批注栏、页脚、面板头部以及常规操作区尽量不使用“方框 + 文字”的按钮：默认无边框、无底色、无阴影，只保留文字或图标本身；鼠标悬浮时只使用中性极淡底色或轻微字重变化提示可点击，不出现描边方框；选中态必须使用浅灰色底和字体加粗，文字颜色不变，不使用整块蓝色、蓝色底条或蓝色文字。保存、发送、导入、翻译等主操作也和普通按钮保持一致，不额外使用重色底或强调色。所有工具按钮必须有 `title` 或等价说明，鼠标悬浮时可以看到功能介绍。

- 默认工具按钮高度：`32px`
- 通用表单高度：`34px`
- 工具按钮圆角：`4px`
- 输入框圆角：`6px`
- 卡片/消息圆角：`8px`
- 默认工具按钮边框：`0` 或 `1px solid transparent`，视觉上必须不可见
- 默认工具按钮背景：`transparent`
- 默认工具按钮悬浮：文字保持 `var(--text)` 或 `var(--ink)`，淡底不超过 `rgba(36, 36, 36, 0.045)`
- 选中工具按钮标识：中性浅灰底 `rgba(36, 36, 36, 0.06)`，字重 `600`
- 选中工具按钮文字：保持普通文字色 `var(--text)`，不要变成蓝色或其他强调色
- 主操作按钮：与普通按钮一致，不使用蓝色文字、蓝色底条或蓝色块
- 主操作按钮悬浮：与普通按钮一致，使用中性极淡底色
- 聚焦描边：`2px solid var(--focus)`

批注栏里的状态工具，例如 `绘制`、`橡皮`，不使用 checkbox 方格、蓝色底条、蓝色文字或重色块，统一做成普通文字/图标工具按钮；状态通过浅灰底、字体加粗和 `aria-pressed` 表达，字体颜色保持不变。当前不提供文本高亮或荧光笔功能，避免低质量标记破坏 PDF 阅读质感。

导航栏和工具栏采用 ribbon 结构：白色或近白背景、底部分割线、紧凑高度、工具组轻微分组。

- 顶部工具栏背景：`rgba(251, 252, 253, 0.96)`
- 批注栏背景：`rgba(255, 255, 255, 0.98)`
- 工具组背景：`rgba(255, 255, 255, 0.88)`
- 工具组边框：`1px solid var(--line-soft)`
- 工具组阴影：`0 1px 2px rgba(15, 23, 42, 0.05)`

抽屉、弹出菜单和浮层必须像 Office 浮动面板：白底、细边框、轻阴影、少量模糊，不做强烈遮罩。

- 抽屉阴影：`0 18px 44px rgba(15, 23, 42, 0.14)`
- 浮层阴影：`0 8px 24px rgba(15, 23, 42, 0.16)`
- 背景遮罩：`rgba(36, 36, 36, 0.16)`
- 菜单内边距：`6px`

PDF 阅读区的页面应当是视觉中心。背景使用浅色混合渐变，页面白底、细边框、轻阴影。批注属于页面内容，笔记文字和手写线随 PDF 缩放；编辑工具条属于操作 UI，应保持小巧清晰。

- 页面背景：`#ffffff`
- 页面边框：`1px solid rgba(0, 0, 0, 0.08)`
- 页面阴影：`0 4px 18px rgba(0, 0, 0, 0.12)`
- 笔记编辑蓝：`#1f7fd1`
- 笔记工具条背景：`rgba(255, 255, 255, 0.97)`

PDF 文本选择只用于翻译、讲解和笔记定位。选区矩形应从 textLayer 的实际文字节点/文字 span 交集计算，按行合并相邻片段，避免跨行粘连；不要借选区实现粗糙色块高亮。

翻译结果和 AI 回答直接展示为正文流，不使用气泡、卡片、左边色条或阴影。AI 讲解栏里只有用户提问使用浅灰色气泡包裹，用来区分提问和回答。

- 用户提问气泡背景：`#f6f7f9`
- 用户提问气泡边框：`1px solid #dde2e8`
- 用户提问气泡圆角：`8px`
- AI 回答背景：`transparent`
- 翻译结果背景：`transparent`

页码滑条使用浅灰体系，不使用重蓝色进度条或蓝色圆点。

- 滑条轨道：`#eef1f4`
- 滑条轨道边框：`#cfd5dd`
- 滑条进度/圆点：`#9aa1aa`
- 滑条焦点：`rgba(96, 94, 92, 0.18)`

滚动条使用中性灰蓝，悬浮时略加深。滚动条不能抢走 PDF 内容注意力。

- 滚动轨道：`rgba(222, 226, 232, 0.86)`
- 滚动滑块：`rgba(96, 94, 92, 0.42)`
- 滚动滑块悬浮：`rgba(96, 94, 92, 0.62)`
