# AI 课件助教需求与设计理念（精简版）

## 1. 文档定位

本文件只描述两件事：

1. 业务需求（做什么）
2. 设计理念（为什么这样做）

实现细节、技术方案、模块拆分、状态机细节请查看独立技术文档：

- [TECHNICAL_ARCHITECTURE_AND_IMPLEMENTATION.md](TECHNICAL_ARCHITECTURE_AND_IMPLEMENTATION.md)
- [DEVELOPMENT_WORKFLOW.md](DEVELOPMENT_WORKFLOW.md)

---

## 2. 核心业务目标

### 2.1 主能力

- 在 VS Code 中提供“课件 AI 助教”侧边栏
- 用户选中文本后，自动触发讲解
- 用户可继续追问（多轮上下文）
- 支持取消当前响应
- 支持图片讲解（整图理解优先，OCR 为兜底）

### 2.2 关键交互语义（强约束）

1. **文本触发语义**
   - 鼠标左键松开且选区最终成立才触发

2. **PDF 触发语义**
   - 目标语义同上：左键松开且最终选区成立
   - 最终方案优先采用自建 PDF 视图事件触发

3. **取消语义**
   - 点击取消后，当前请求必须停止
   - 取消后不得出现旧请求晚到输出

### 2.3 非功能目标

- 触发延迟尽量低（亚秒级体验）
- 误触发率可控
- 配置可扩展，适配不同模型供应商
- 代码可维护、可测试

---

## 3. 设计理念

### 3.1 语义优先

先满足“用户感知正确”再追求实现优雅：

- 先保证触发时机正确
- 再优化延迟
- 最后做代码结构收口

### 3.2 事件驱动优先于轮询

- 普通文本场景走事件触发
- PDF 场景目标走自建视图事件触发
- 轮询仅作为过渡或兼容兜底，不作为长期主路径

### 3.3 可取消、可回收、可丢弃

取消不是“尽量取消”，而是完整的生命周期控制：

- 可中断进行中请求
- 可判定旧请求失效
- 可丢弃迟到回包

### 3.4 分层与边界清晰

- 触发层只采集事件，不做业务决策
- 编排层统一决策与状态推进
- Provider 层只负责模型通信
- UI 层只负责展示和交互

### 3.5 配置可移植

模型网关与协议可配置，避免业务代码绑定某一供应商。

---

## 4. 验收口径（需求视角）

### 4.1 触发

- 普通文本：左键松开后触发一次
- PDF：左键松开后触发一次（主路径为自建 PDF 视图）
- 拖选过程中不触发

### 4.2 取消

- 取消后状态即时反馈
- 取消后不再出现旧输出

### 4.3 稳定性

- 自动触发与手动触发不相互干扰
- 连续操作下无明显“卡死/乱序/鬼触发”

---

## 5. 后续文档关系

- 本文档：需求与设计理念（What + Why）
- 技术文档：实现方案与模块设计（How）
- 流程文档：开发-测试-回退闭环（Execution）
